1. Install Mongo DB

2. Install pymongo
     sudo apt install python3-pip
     pip3 install pymongo
   
3. Initialize DB
     cd fisb-decode
     mongo db/scripts/createFisb.js

4. Other Misc Installs
     pip3 install shapely
     pip3 install tzlocal
     pip3 install geographiclib

5. Install Sphinx
     sudo apt install python3-sphinx
     pip3 install sphinxcontrib-napoleon
     pip3 install python-docs-theme

install texlive-latex-extra (probably texlive too?)
  make latexpdf

PYTHONPATH=/share/fisb-decode
export PYTHONPATH
